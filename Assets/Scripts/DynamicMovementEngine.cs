using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DynamicMovementEngine : MonoBehaviour
{
    // Should extend a class or interface that has 4 directions for movement to apply forces
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }


}
